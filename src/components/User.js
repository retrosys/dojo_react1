import React from 'react'

const User = ({ name, id, add }) => (
 //   return (
        <div className="yolo"> 
         <span>{ name }</span>
         <button onClick={() => add(id)}>Add</button>
        </div>
   // )
//}
)
export default User